# NATS Server Configuration
# Managed by Ansible - will be updated after operator creation via NIS

port: 4222
http_port: 8222

jetstream {
  store_dir: /data/jetstream
  max_mem: 1G
  max_file: 10G
}
